---
import Layout from "../layouts/Layout.astro";
import { zodCollectionsWithRichData } from "@/content/config";

interface Props {
  title: string;
  classes?: string;
  frontmatter?: any;
  page?: any;
}

let { title, classes, } = Astro.props;
let { page, itemClass } = Astro.params;

---
<Layout title={title}>
  <slot/>
</Layout>
